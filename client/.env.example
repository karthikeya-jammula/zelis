# For local development when running the client standalone (without the Vite
# proxy pointing at the server). In normal dev mode (npm run dev from root)
# the proxy handles /api automatically and this variable is not needed.
VITE_API_BASE_URL=http://localhost:5000/api
